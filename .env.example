# Database (replace when the persistence layer is introduced)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/financial_associate"

# AI provider credentials are server-only. Never expose them with NEXT_PUBLIC_.
OPENAI_API_KEY=""

